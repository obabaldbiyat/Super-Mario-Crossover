node{

    stage('Clone') {
        git credentialsId: '10fedfbb-8a4e-4700-b3cd-3ad56c87c166', url: 'https://github.com/obabaldbiyat/Super-Mario-Crossover'
    }
        stage('Build docker image'){
            steps{
                script{
                    sh 'sudo docker build -t obab/SuperMario .'
                }
            }
        }
        stage('Push image to Hub'){
            steps{
                script{
                   withCredentials([string(credentialsId: 'dockerhub-pwd', variable: 'dockerhubpwd')]) {
                   sh 'docker login -u obab -p ${dockerhubpwd}'

}
                   sh 'sudo docker push obab/SuperMario'
                }
            }
        }
    }
}
